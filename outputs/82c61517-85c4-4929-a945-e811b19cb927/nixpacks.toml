[phases.setup]
nixPkgs = ["...", "python311", "playwright-driver"]

[phases.install]
cmds = ["pip install -r requirements.txt", "playwright install --with-deps chromium"]

[start]
cmd = "gunicorn --worker-class sync --workers 1 --bind 0.0.0.0:$PORT --timeout 600 app:app"
